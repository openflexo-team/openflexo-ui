# $generator.getFMLKeyword()
[//]: # (Do not edit this file, which is automatically generated)

$generator.getBigIconAsHTML() $generator.toMD($generator.getFMLDescription())

---

#[[##]]# Usage

```java
$generator.toCode($generator.getUsage(false))
```

or

```java
$generator.toCode($generator.getUsage(true))
```

where

- `visibility` is default (unspecified), 'public', 'protected' or 'private'
- `cardinality` is \[0,1\] (unspecified), \[0,\*\] or \[1,\*\]
- \<identifier\> is the name of declared model slot variable
#foreach($fmlProperty in $generator.getFMLProperties())
#if ($fmlProperty.isRequired())
- \<$fmlProperty.getPathNameInUsage()\> addresses a `$generator.getPropertyTypeAsString($fmlProperty)`
#end
#end

---

#[[##]]# Configuration

| Property        | Type                    | &nbsp;Required&nbsp;  |
| --------------- |-------------------------| :------:|
#foreach($fmlProperty in $generator.getFMLProperties())
| `$fmlProperty.getLabel()` &nbsp; | `$generator.getPropertyTypeAsString($fmlProperty)` &nbsp; | #if ($fmlProperty.isRequired()) yes #else no #end |
#end

---

#foreach($fmlProperty in $generator.getFMLProperties())
- `$fmlProperty.getLabel()` : $generator.toMD($fmlProperty.getDescription())
#end

---

#[[##]]# Examples

#foreach($usageExample in $generator.getFMLExamples())
```java
$generator.toCode($usageExample.example())
```

$generator.toMD($usageExample.description())
#end

[//]: # --------------------- ROLES
#set ($availableFlexoRoleTypes = $generator.getTechnologyAdapterService().getAvailableFlexoRoleTypes($generator.getObjectClass()))
#if ($availableFlexoRoleTypes.size()>0)
---

#[[##]]# Roles

#foreach($roleClass in $availableFlexoRoleTypes)
#set ($generatorReference = $generator.getReference($roleClass))
- $generatorReference.getSmallIconAsHTML() [$generatorReference.getFMLKeyword()]($generatorReference.getLocalMDPath()) : $generator.toMD($generatorReference.getFMLShortDescription())
#end
#end

[//]: # --------------------- BEHAVIOURS
#set ($availableFlexoBehaviourTypes = $generator.getTechnologyAdapterService().getAvailableFlexoBehaviourTypes($generator.getObjectClass()))
#if ($availableFlexoBehaviourTypes.size()>0)
---

#[[##]]# Behaviours

#foreach($behaviourClass in $availableFlexoBehaviourTypes)
#set ($generatorReference = $generator.getReference($behaviourClass))
- $generatorReference.getSmallIconAsHTML() [$generatorReference.getFMLKeyword()]($generatorReference.getLocalMDPath()) : $generator.toMD($generatorReference.getFMLShortDescription())
#end
#end

[//]: # --------------------- EDITION ACTIONS
#set ($availableEditionActionTypes = $generator.getTechnologyAdapterService().getAvailableEditionActionTypes($generator.getObjectClass()))
#if ($availableEditionActionTypes.size()>0)
---

#[[##]]# Edition actions

#foreach($editionActionClass in $availableEditionActionTypes)
#set ($generatorReference = $generator.getReference($editionActionClass))
- $generatorReference.getSmallIconAsHTML() [$generatorReference.getFMLKeyword()]($generatorReference.getLocalMDPath()) : $generator.toMD($generatorReference.getFMLShortDescription())
#end
#end

[//]: # --------------------- FETCH REQUEST
#set ($availableFetchRequestsTypes = $generator.getTechnologyAdapterService().getAvailableFetchRequestTypes($generator.getObjectClass()))
#if ($availableFetchRequestsTypes.size()>0)
---

#[[##]]# Fetch requests

#foreach($fetchRequestClass in $availableFetchRequestsTypes)
#set ($generatorReference = $generator.getReference($fetchRequestClass))
- $generatorReference.getSmallIconAsHTML() [$generatorReference.getFMLKeyword()]($generatorReference.getLocalMDPath()) : $generator.toMD($generatorReference.getFMLShortDescription())
#end
#end

---

#[[##]]# Javadoc

[org.openflexo.technologyadapter.diagram.TypedDiagramModelSlot](./apidocs/org/openflexo/technologyadapter/diagram/TypedDiagramModelSlot.md)

